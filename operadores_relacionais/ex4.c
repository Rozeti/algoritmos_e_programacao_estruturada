#include<stdio.h>

main(){// EX de operadores relacionais

    double peso;

    printf("Digite o seu peso: ");
    scanf("%lf", &peso);

    if (peso >= 60.00)
    {
        printf("Voce pesa mais que 60.00kg");
    }
    else{
        printf("Voce pesa mais que 60.00kg");
    }
}
